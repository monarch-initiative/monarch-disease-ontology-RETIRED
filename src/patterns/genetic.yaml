pattern_name: genetic

classes: []

relations: 

vars:
  disease: "Thing"

name:
  text: "genetic %s"
  vars:
   - disease

annotations:
  - property: oio:hasExactSynonym
    text: "genetic %s"
    vars:
     - disease

def:
  text: "Genetic %s."
  vars:
    - disease

equivalentTo:
  text: "%s and 'has_status' some 'genetic'"
  vars:
    - disease


write_yaml	genetic
